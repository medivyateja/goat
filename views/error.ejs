<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - GOAT Token</title>
    <link rel="stylesheet" href="https://rsms.me/inter/inter.css">
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        body {
            background: #273296;
            font-family: 'Inter', sans-serif;
        }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center">
    <div class="max-w-xl mx-auto px-6 py-16 text-center">
        <h1 class="text-4xl font-bold text-white mb-4">
            <%= error.status === 404 ? '404 - Page Not Found' : 'Oops! Something went wrong' %>
        </h1>
        <p class="text-xl text-gray-300 mb-8"><%= message %></p>
        <% if (process.env.NODE_ENV === 'development' && error.stack) { %>
            <pre class="mt-8 p-4 bg-blue-900 rounded-lg text-left text-sm text-gray-300 overflow-x-auto">
                <%= error.stack %>
            </pre>
        <% } %>
        <a href="/" class="inline-block px-8 py-3 rounded-full bg-red-600 text-white font-semibold hover:bg-red-700 transition-colors">
            Back to Home
        </a>
    </div>
</body>
</html>